export interface Course {
  id: string;
  faculty: string;
  department: string;
  degree: Date;
  subject: string;
  subjectCode: string;
  modality: string;
  teacherId: string;
  academicPeriod: string;
  academicLevel: string;
  createdAt: Date;
}
